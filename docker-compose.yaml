version: '3'

services:

  app:
    hostname: app
    build: './src/app'
    volumes:
      - './src/app:/go'
    ports:
      - '8080:80'

  docker-push:
    image: 'tmvdl/projects:[project]'
    build: './src/app'

  github-release:
    image: 'github-release'
    build: './src'
